#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Where would you like to go?"

input = gets.strip

def input_to_index (input)
 string_to_int = "#{input}".to_i
 index = (string_to_int) - 1

move(board, index, value)

display_board(board)